{
    "github_token": {
        "name": "GitHub Token",
        "type": "string",
        "default": "",
        "hint": "GitHub Personal Access Token (需有repo权限)",
        "locales": {
            "zh": { "name": "GitHub Token", "hint": "GitHub Personal Access Token（需要有 repo 权限）" }
        }
    },
    "watch_enabled": {
        "name": "启用自动监控",
        "type": "switch",
        "default": false,
        "hint": "是否开启PR/Issue自动检查",
        "locales": {
            "zh": { "name": "启用自动监控", "hint": "是否开启PR/Issue自动检查" }
        }
    },
    "watch_search_mode": {
        "name": "全量搜索模式",
        "type": "switch",
        "default": false,
        "hint": "开启后不按仓库列表扫描，直接搜所有我提的PR和Issue（跨仓库全量）",
        "locales": {
            "zh": { "name": "全量搜索模式", "hint": "开启后不按仓库列表扫描，直接搜所有我提的PR和Issue（跨仓库全量）" }
        }
    },
    "watch_interval_type": {
        "name": "定时模式",
        "type": "select",
        "default": "interval",
        "options": [
            { "label": "固定时间间隔", "value": "interval" },
            { "label": "固定时间点", "value": "fixed_time" }
        ],
        "hint": "interval=每隔N分钟检查一次；fixed_time=按cron表达式触发",
        "locales": {
            "zh": { "name": "定时模式", "hint": "interval=每隔N分钟检查一次；fixed_time=按cron表达式触发" }
        }
    },
    "watch_interval_minutes": {
        "name": "检查间隔(分钟)",
        "type": "integer",
        "default": 60,
        "min": 5,
        "max": 1440,
        "hint": "interval模式下每隔多少分钟检查一次",
        "locales": {
            "zh": { "name": "检查间隔(分钟)", "hint": "interval模式下每隔多少分钟检查一次" }
        }
    },
    "watch_own_repos": {
        "name": "监控所有自有仓库",
        "type": "switch",
        "default": true,
        "hint": "自动监控token所属用户的所有仓库（search_mode关闭时生效）",
        "locales": {
            "zh": { "name": "监控所有自有仓库", "hint": "自动监控token所属用户的所有仓库（search_mode关闭时生效）" }
        }
    },
    "watch_repos": {
        "name": "额外监控仓库",
        "type": "list",
        "default": [],
        "hint": "额外监控的仓库，每行一个，格式: owner/repo（search_mode关闭时生效）",
        "locales": {
            "zh": { "name": "额外监控仓库", "hint": "额外监控的仓库，每行一个，格式: owner/repo（search_mode关闭时生效）" }
        }
    },
    "watch_issues": {
        "name": "监控Issue",
        "type": "switch",
        "default": true,
        "hint": "是否同时监控Issue回复",
        "locales": {
            "zh": { "name": "监控Issue", "hint": "是否同时监控Issue回复" }
        }
    },
    "watch_auto_fix": {
        "name": "自动修复",
        "type": "switch",
        "default": false,
        "hint": "收到review意见后自动修改代码（需实现LLM分析逻辑）",
        "locales": {
            "zh": { "name": "自动修复", "hint": "收到review意见后自动修改代码（需实现LLM分析逻辑）" }
        }
    },
    "watch_require_confirm": {
        "name": "需要确认",
        "type": "switch",
        "default": true,
        "hint": "为true时只发通知不给执行，为false且auto_fix开启时自动修改",
        "locales": {
            "zh": { "name": "需要确认", "hint": "为true时只发通知不给执行，为false且auto_fix开启时自动修改" }
        }
    },
    "watch_notify_target": {
        "name": "通知目标会话",
        "type": "list",
        "default": [],
        "hint": "通知目标会话ID，每行一个，格式: qq:dm:QQ号 或 qq:gm:群号",
        "locales": {
            "zh": { "name": "通知目标会话", "hint": "通知目标会话ID，每行一个，格式: qq:dm:QQ号 或 qq:gm:群号" }
        }
    }
}
